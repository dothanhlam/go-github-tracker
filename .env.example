# Environment Configuration Example
# 
# This project supports multiple environment files:
# - .env.local      (local development - default)
# - .env.test       (testing)
# - .env.staging    (staging environment)
# - .env.production (production - or just .env)
#
# The environment is selected via the APP_ENV variable:
# - If APP_ENV is not set, defaults to "local"
# - The app will load .env.{APP_ENV} (e.g., .env.local)
# - For production, you can use .env or .env.production
#
# Usage:
#   APP_ENV=local ./bin/collector      (uses .env.local)
#   APP_ENV=test ./bin/collector       (uses .env.test)
#   APP_ENV=production ./bin/collector (uses .env.production or .env)
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local

# Database Configuration
# Use 'sqlite3' for local development, 'postgres' for production
DB_DRIVER=sqlite3
DB_URL=./data/dora_metrics.db

# For PostgreSQL (production):
# DB_DRIVER=postgres
# DB_URL=postgres://user:password@localhost:5432/dora_metrics?sslmode=disable

# GitHub Configuration
GITHUB_PAT=ghp_your_personal_access_token_here

# Team Configuration (JSON array)
# Example with multiple teams and weighted allocations
TEAM_CONFIG_JSON=[{"team_id":1,"name":"Platform","members":[{"username":"alice","allocation":1.0},{"username":"bob","allocation":0.5}]},{"team_id":2,"name":"Frontend","members":[{"username":"bob","allocation":0.5},{"username":"charlie","allocation":1.0}]}]

# Repositories to track (comma-separated, format: owner/repo)
REPOSITORIES=owner/repo1,owner/repo2,owner/repo3
